version: "3.4"

services:
  north_client:
    build:
      context: .
      target: app_node
      network: host
    restart: unless-stopped
    volumes:
      - ./data:/app/data
      - ./dbbackup:/app/dbbackup
    environment:
      - DEPLOYED_IN_DOCKER=true
    ports:
      - "80:9000"
